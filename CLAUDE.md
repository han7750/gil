# CLAUDE.md

이 파일은 클로드 코드가 이 폴더에서 일할 때 자동으로 읽습니다.
다른 컴퓨터에서 이 저장소를 내려받아도 여기 적힌 내용은 그대로 따라옵니다.

## 이 사람에 대해

- **개발자가 아닙니다.** 항상 초보자에게 설명하듯 말해주세요.
- **모든 대답은 한국어로만.** 영어 기술 용어를 쓰지 마세요. 꼭 필요하면 옆에 쉬운 말로 풀어주세요.
- '미안합니다', '사과합니다' 같은 표현을 쓰지 마세요.
- 길고 자세하게, 단계별로, 표와 비유를 써서 설명해주세요.
- **왜 그렇게 하는지 이유도 항상 함께** 말해주세요.
- 되돌릴 수 없는 작업은 먼저 확인을 받으세요.
- 답변 끝에 후속 질문 3개를 **Q1**, **Q2**, **Q3** 형식으로 붙여주세요.
- 코드를 보여줄 때는 각 줄이 무슨 뜻인지 주석으로 설명해주세요.

## 이 프로젝트가 무엇인가

**「road」** — 소상공인에게 **무료로** 광고 영상 제작안을 만들어 드리는 사이트입니다.
2026-08-03에 만들고, 2026-08-07에 기업 사이트처럼 다시 디자인했습니다.

**이름 주의**: 화면에 보이는 상표 이름은 전부 **road** 입니다 (2026-08-07 변경).
폴더 이름과 저장소 경로만 예전 `길` 그대로입니다 — 폴더를 바꾸면 깃 연결이 끊어지기 때문입니다.

> ⚠ 코드 안에서 `"길:"` 과 `"길자물쇠:"` 두 곳은 **절대 바꾸지 마세요.**
> 열쇠를 만드는 데 쓰이는 속 이름이라, 바꾸면 이미 보낸 결과가 안 열리고
> 운영실 비밀번호도 막힙니다. `node _검사.js` 의 52번이 이걸 지켜보고 있습니다.

신청서 표시도 `[road 신청서]` / `[ROAD REQUEST]` 로 바뀌었지만,
예전 표시(`[길 신청서]` / `[GIL REQUEST]`)로 보낸 신청서도 그대로 읽힙니다.

- 사이트 주소: https://han7750.github.io/gil/
- 이 저장소가 그대로 사이트입니다 (깃허브 페이지)

## 서비스 방식 (2026-08-07 바로잡음 — 아주 중요)

**사장님은 찍지 않습니다. 편집도 하지 않습니다.**
가게 이야기만 적어 보내주시면, **내가 클로드를 써서 완성된 광고 영상을 직접 만들어** 카카오톡으로 보내드립니다.
소재도 내가 준비하므로 사장님은 사진 한 장 보내지 않아도 됩니다.

| | 사장님 | road (나) |
|---|---|---|
| 하는 일 | 가게 이야기 적어서 보내기 (2분) | 영상 제작 전부 |
| 안 해도 되는 일 | 촬영 · 편집 · 소재 준비 · 자막 넣기 | — |

받는 것: **완성된 광고 영상** · 자막이 얹힌 상태 · 같이 올릴 광고 문구 · 해시태그 · 무료 수정

> ⚠ **글을 쓸 때 절대 "찍는 법을 알려드립니다" 식으로 쓰지 마세요.**
> 예전에 그렇게 쓰여 있어서 바로잡았습니다. `node _화면시늉.js` 가
> "찍는 건 사장님 / 촬영 지시 / 준비물 / 편집 순서 / 캡컷" 같은 말이 다시 들어오면 잡아냅니다.

### 결과에 담는 것 (2026-08-07 정리)

영상은 **파일로 따로** 보내드리므로, 결과 화면에는 **같이 올릴 것만** 둡니다.

| 남긴 것 | 뺀 것 |
|---|---|
| **영상에 쓴 문구** (`videoCopy`) — 이미 영상 안에 들어간 한 줄 | 대본 (컷별 화면) |
| **게시글에 쓸 문구** (`postCopy`) — 올릴 때 밑에 붙일 글 3안 | 촬영 목록 · 촬영 요령 |
| 자막 (영상에 들어간 순서대로) | 준비물 |
| 해시태그 | 캡컷 · 블로 편집 순서 |

문구를 둘로 나눈 이유: 완성 영상을 드리므로 **문구 하나는 이미 영상에 쓰였습니다.**
"3안 중에 고르세요"라고 하면 사장님이 뭘 해야 하는지 헷갈립니다.
영상에 쓴 것은 **보고**, 게시글용은 **골라 쓰는 것** 으로 나눠 놨습니다.

- 결과 화면 탭이 5개 → **3개** (`plan` 광고 문구 / `caption` 자막 / `tags` 해시태그)로 줄었습니다.
- 클로드 지시문도 `props` · `shootTips` · `edit` · `shot` 을 **더 이상 만들지 않게** 바꿨습니다.
> ⚠ **`fillResult()` 는 두 곳에서 반드시 불러야 합니다** — 결과를 새로 만들 때(`parseResult`)와
> **예전 결과 코드를 열 때(`unpack`)**. 한쪽만 부르면, 이미 사장님께 보낸 결과 코드를 여는 순간
> 화면이 그대로 멈춥니다. 실제로 이 실수가 한 번 났고 `_화면시늉.js` 가 잡아냈습니다.
> 옛 항목(`props`·`shootTips`·`edit`)의 기본값도 같은 이유로 남겨 둔 것이니 지우지 마세요.

### 가게 사진 안내

"사진을 보내야 한다"가 아니라 **"보내면 더 좋다"** 로만 씁니다.
사진 없이도 영상은 나오지만, 있으면 어느 가게나 비슷한 영상이 되는 것을 막아 줍니다.
안내는 두 곳에 있습니다 — 이야기 적는 화면(`brief`), 신청서 보내는 화면(`sheet`, 카톡 여는 자리 바로 밑).
낱말은 `photoTip` · `photoNote`.

## 고치고 올리는 법

**`index.html` 하나만 고치면 됩니다.** 복사할 필요 없습니다.

**`_점검.bat` 을 두 번 클릭하면 검사가 한 번에 다 돕니다.** 이게 가장 쉬운 방법입니다.

```
_점검.bat              # 검사 세 가지를 한 번에 (두 번 클릭해도 됨)
git add -A
git commit -m "무엇을 고쳤는지"
git push               # 1~2분 뒤 사이트에 반영됩니다
```

`_점검.bat` 이 하는 일:

| | 무엇을 보나 |
|---|---|
| `_검사.js` | 빠진 게 없나 **글자로 훑어봄** (52가지) |
| `_화면시늉.js` | 코드를 **진짜로 돌려서** 화면이 뜨는지 봄 |
| `_미리보기그림` | 카톡 미리보기 그림이 최신인지 보고, 낡았으면 새로 만듦 |

★ 표시가 하나라도 나오면 **올리지 마세요.** 통과하면 올리는 방법까지 화면에 안내가 나옵니다.
통과한 항목까지 전부 보고 싶으면 `node _점검.js 자세히`.

> **검사 두 개는 보는 것이 다릅니다.** `_검사.js` 는 글자만 훑기 때문에
> "화면이 실제로 뜨는가"는 절대 알 수 없습니다. 실제로 결과 코드가 안 열리던 사고를
> `_화면시늉.js` 가 잡아냈습니다. 하나만 돌리지 마세요.

### 카톡 미리보기 그림(`og.png`) 을 바꾸려면 — 두 번, 순서대로

```
node _미리보기그림.js               # ① 바탕 · road 글자 · 차선
powershell -File _미리보기그림.ps1   # ② 그 위에 한 줄 글
```

**왜 둘로 나눠져 있나**: 한글은 곧은 막대로 그릴 수 없어 진짜 글꼴이 필요한데,
Node 에는 글꼴이 없고 윈도우에는 있습니다. 그래서 그림은 Node 가, 글자는 윈도우가 그립니다.
**①만 돌리면 글이 사라집니다. 반드시 ②까지 돌리세요.**

`.ps1` 파일은 **BOM 붙은 UTF-8** 이어야 합니다. 그 표시가 없으면 윈도우 파워셸이
한글을 깨뜨려 읽어서 실행이 멈춥니다.

> ⚠️ **파일 이름을 `index.html` 에서 바꾸면 사이트가 죽습니다.** 깃허브 페이지가 이 이름을 찾습니다.

## 문이 세 개인 구조

브라우저에서 인공지능을 직접 부를 수 없고, 열쇠를 코드에 넣으면 누구나 꺼내 씁니다.
그래서 **사람이 중간에 들어가는 구조**로 짰습니다.

| 문 | 누가 | 하는 일 |
|---|---|---|
| ① 앞문 | 사장님 | 가게 이야기 적고 신청 → 신청서가 만들어짐 → 카톡으로 보냄 |
| ② 운영실 | 나 (비밀번호로 잠김) | 신청서 읽기 → 지시문 복사 → 클로드 답 붙여넣기 → 결과 만들기 |
| ③ 결과 문 | 사장님 | 카톡에서 바로 읽거나, 코드+열쇠번호로 예쁜 화면 열기 |

## 반드시 지켜야 할 것들

### 유료 등급은 잠가 둡니다
`TIERS.fast.locked = true` — **건드리지 마세요.**
지금은 돈을 받지 않기로 했습니다. 개인 사정이 정리될 때까지 잠급니다.
다시 열 때는 `locked` 를 `false` 로만 바꾸면 됩니다.

### 결과는 열쇠번호로 잠깁니다
결과 코드는 그 자체로 내용을 다 담고 있어서, 잠그지 않으면 코드를 주운 사람이 사장님 가게 이야기를 그대로 봅니다.
`AES-GCM` 으로 잠그고, 열쇠번호 4자리는 코드와 **따로** 카톡으로 보냅니다.

### 운영실 비밀번호는 되섞은 값만 적습니다
저장소가 공개라 비밀번호를 그대로 적으면 안 됩니다.
`OP_LOCK` 에는 `PBKDF2` 로 20만 번 되섞은 값(`v1:소금:값`)만 들어갑니다.

**비밀번호를 바꾸려면**: 사이트 → 운영실 → 비밀번호 → 새로 정하면 화면이 새 줄을 내줍니다.
그 줄로 `OP_LOCK` 을 갈아 끼우고 올리면 됩니다.
**비밀번호 자체를 물어보지 마세요.** 잠금값 한 줄만 받으면 됩니다.

### 결과 링크는 진짜 주소에서만 작동합니다
`SITE_URL` 에 적힌 주소로 링크를 만듭니다.
클로드 아티팩트처럼 액자(iframe) 안에서 열리면 바깥 주소의 `#` 뒷부분을 못 읽어서 링크가 작동하지 않습니다.
그래서 **결과 전문을 카톡으로 그대로 보내는 방법**이 기본입니다. 사장님이 누를 게 없습니다.

## 디자인 (2026-08-07 새로 짬)

크림색 바탕 + 주황 강조 + 아주 굵고 큰 제목. 카드와 단추는 둥글게.
색과 모양은 **`<style>` 맨 위 `:root` 한 곳에만** 적혀 있습니다. 거기만 바꾸면 사이트 전체가 바뀝니다.

| 이름 | 뜻 | 밝은 화면 값 |
|---|---|---|
| `--road` | 바탕색 | `#FBF4E9` (크림) |
| `--paper` | 글자색 | `#191310` (거의 검정) |
| `--panel` | 카드 속색 | `#FFFFFF` |
| `--accent` | 강조 주황 (단추) | `#E2571E` |
| `--accent-ink` | 크림 위에 쓰는 진한 주황 | `#B8420F` |
| `--onAccent` | 주황 단추 위 글자색 | `#FFF7EE` |
| `--r2` | 카드 모서리 둥글기 | `22px` |

어두운 화면 값은 바로 아래 `@media (prefers-color-scheme:dark)` 에 짝으로 있습니다.
**한쪽만 고치면 밤에 글자가 안 보입니다. 반드시 양쪽을 같이 고치세요.**

### 첫 화면은 기업 사이트 구조입니다
`SCREENS.intro` 하나가 아래 여덟 덩이를 그립니다.
머리띠(`.nav`) → 첫인상(`.hero`) → 흐르는 띠(`.marquee`) → 받는 것 → 진행 순서 →
왜 무료인가 → 자주 묻는 질문 → 마지막 권유 → 바닥글.

- 첫 화면일 때만 `.wrap` 에 `home` 이 붙어 화면 전체 폭을 씁니다. 나머지 화면은 좁은 종이 폭 그대로입니다.
- 조각을 만드는 도우미 함수가 있습니다: `sec()` `secHead()` `gcard()` `bigStep()` `faqItem()`.
  새 구역을 넣을 때 이걸 쓰면 모양이 저절로 맞습니다.
- **글은 절대 코드에 직접 적지 말고 `T.ko` / `T.en` 에 넣고 `t("이름")` 으로 부르세요.**

## 코드 안에서 자주 고치는 자리

| 찾을 말 | 무엇 |
|---|---|
| `SITE_URL` | 사이트의 진짜 주소 |
| `OP_LOCK` | 운영실 비밀번호 잠금값 |
| `CONTACT_DEFAULT` | 신청서를 받을 카카오톡·이메일·인스타 |
| `TIERS` | 납기 종류 (`locked:true` 면 못 고름) |
| `LENGTHS` | 고를 수 있는 영상 길이와 컷 수 |
| `REPLIES` | 카톡 답장 문구 모음 |
| `T.ko` / `T.en` | 화면에 나오는 모든 말 (한국어·영어 짝이 맞아야 함) |

## 한국어와 영어

외국인 사장님도 받습니다.

- 오른쪽 위 **KO / EN** 으로 바꾸고, 고른 말을 기억합니다
- 영어 신청서는 `[ROAD REQUEST]` 형식입니다 (예전 `[GIL REQUEST]` 도 계속 읽힘)
- **운영실이 영어 신청서를 읽으면 자동으로 영어 모드가 됩니다** → 지시문·결과 글까지 전부 영어
- 영어 화면에서는 이메일·인스타그램 디엠을 먼저 보여줍니다 (외국인은 카톡을 안 씀)

**새 문구를 넣을 때는 반드시 `T.ko` 와 `T.en` 양쪽에 넣으세요.**
`node _검사.js` 의 29번이 짝이 맞는지 확인해 줍니다.

## 장부는 이 컴퓨터에만 있습니다

결과를 만들 때마다 날짜·가게·납기·**열쇠번호**·결과 코드가 자동으로 기록됩니다.
브라우저 안에만 저장되고 인터넷으로 나가지 않습니다.

**그래서 컴퓨터를 바꾸면 장부가 따라오지 않습니다.**
옮기려면: 예전 컴퓨터에서 운영실 → 장부 → **백업 글 복사** → 새 컴퓨터에서 → **붙여넣은 백업 되살리기**

## 이어서 만든 것

`광고작업실` 프로젝트(https://github.com/han7750/ad-workshop, 비공개)를 만들다가,
브라우저에서 인공지능을 직접 부를 수 없다는 걸 알고 이 구조로 다시 짰습니다.

## 카카오 채널

- 채널 채팅: https://pf.kakao.com/_uUxbBX/chat (끝이 `/chat` 이어야 채팅창이 바로 열립니다)
- 관리자센터: https://center-pf.kakao.com
- 넣을 문구: `카카오채널-세팅.html` 을 열면 복사 단추로 정리돼 있습니다

**주의**: 채널 이름은 친구 100명 이하일 때 **1회만** 바꿀 수 있습니다. 되돌릴 수 없습니다.
